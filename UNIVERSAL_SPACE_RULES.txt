UNIVERSAL SPACE - PROJECT RULES
Recorded: 1 September 2026
Owner: devmaster / 999nike

READ THIS FIRST. Maintain this file in place; do not create competing copies.
Canonical repository path: UNIVERSAL_SPACE_RULES.txt on molecular-v2.
GitHub publication does not establish installation or branch repair on the HP.

THE REQUIRED SETUP

Repository: https://github.com/999nike/memory-app

main
  The conventional, normal interface. Preserve it.
  Do not put spatial experiments, cleanup commits or restored spatial snapshots
  on this branch. Do not merge the spatial version into main without the user
  explicitly requesting it.

molecular-v2
  The ONE active development/testing branch for the new spatial interface.
  This is the intended branch for the working folder below.

Working folder: E:\WIZZ-Server\new-version\universal-space
Test address: http://127.0.0.1:4173

When the user says "main folder" or "root", they mean this working directory,
NOT the Git branch main. Do not switch to main because of that wording.

CURRENT CONFIRMED BASELINE - 1 SEPTEMBER 2026

User-provided HP terminal output confirms:
- Working folder: E:\WIZZ-Server\new-version\universal-space
- Checked-out branch: molecular-v2, tracking origin/molecular-v2.
- HEAD and origin/molecular-v2 match at:
  4694b417c989b2ddd89a2c50e7849979a9c471b0
- git status --short --branch printed no modified or untracked paths.
- origin is https://github.com/999nike/memory-app.
- The previous restored working files were saved in the Git stash named
  "HP restored state before molecular-v2 update".
- Local restoration commit b112af3 was preserved under the local tag
  restored-before-neural-update before the successful branch alignment.
- The linked bank worktree was last reported detached at 9535bc9.

The user has now accepted 4694b41 as the working neural visual baseline and
explicitly requested it be retained as the rollback version on molecular-v2.
This is the starting point for further work toward the reference picture,
not a claim that the target design is finished. HP FPS has not been measured.
The local screenshot shows Memory, EMAIL and Code Space joined by the finer
branching central trunk, with Settings separate.

Rollback application commit: 4694b417c989b2ddd89a2c50e7849979a9c471b0
Use the existing branch history and baseline record in
UNIVERSAL_SPACE_LEDGER.md. Do not create another project copy or branch.
Preserve subsequent work before any explicitly requested rollback; prefer a
reviewed forward restoration commit on molecular-v2 rather than rewriting
shared history or discarding local changes.

GitHub molecular-v2 was independently checked at that same commit before
recording this baseline. GitHub main was checked at
8de6f6fcc713f756ac2985402c4e176acb6279ec and is not a target of this work.
This supersedes the earlier dirty-main/missing-origin reconciliation reports.
Other folders have not been cleaned or retired. No Windows filesystem changes
are claimed by this GitHub documentation update.

STRICT WORKSPACE RULES

1. No new project copies, sibling development folders, branches or worktrees
   unless the user explicitly requests them. Audit existing extras and unique
   work before proposing retirement; do not delete them by name alone.
2. E:\junkz backup is the user's private backup. Do not read, scan, write,
   move or delete anything there unless the user explicitly requests it.
3. No automatic backups anywhere. Existing backup folders are not automatic
   destinations or permission to create more snapshots.
4. Required runtime/configuration files stay where the working app needs them.
   Do not reorganize live JavaScript as part of documentation cleanup.
5. This rules file and a small AGENTS.md entry point may live in the root.
   AGENTS.md must instruct agents to read this file and the actual handoff;
   do not duplicate the branch policy across multiple conflicting documents.
6. Use docs/research/NEURAL_CONNECTOR_RESEARCH_PLAN.md and
   docs/operations/UNIVERSAL_SPACE_LOCATIONS_AND_BASELINE_2026-09-01.md.
   Put other maintained documentation in docs/ after an explicit move list
   and reference updates. Preserve existing filenames. Until those moves are
   completed, read the existing root UNIVERSAL_SPACE_HANDOFF.md and
   UNIVERSAL_SPACE_LEDGER.md; do not assume proposed docs paths exist.
7. Use the system temporary directory for disposable output. No root-level
   audit dumps, screenshots, patch scripts, spare renderers or backup files.
8. Trace static and dynamic loading, imports, server routes and external use
   before removing a candidate. Untracked does not mean unused. HTTP 200 from
   a static server does not mean the browser executes the file.
9. No additional renderer overlays without explicit approval. Preserve graph
   topology, physics, drag/drop, positions, persistence and app behavior during
   visual work. One coherent patch, followed by an actual visual check.
10. Before editing, verify the actual working directory and branch against
    this file. Report a mismatch and resolve it within the user's authorized
    scope before ordinary development. Do not hide it behind another copy.
11. If a Git command fails, state the exact error in one sentence and stop.
    No speculative resets, force pushes, merges or rebases to bypass it.
12. Never describe a change as applied, committed, pushed, deployed, tested or
    visible until the relevant command or observation proves it. Until then,
    say it is only being attempted. If proof is blocked or unavailable, state
    that immediately and ask the user for direction rather than guessing.

COMPLETION EVIDENCE

For banking/reconciliation, report: working path, checked-out branch, commit,
verified remote target, remaining dirty/untracked files, and whether main's
reference changed. Do not describe intended state as verified state.

For cleanup, report exact moves/removals, updated references and the local
browser check. Do not claim an audit, a push or a rules file alone cleaned up
the folders or established the correct branch.
